
== Migration steps

This section takes building a video conferencing app as an example and introduces the differences between implementing the {VSDK} v{VSDK_RELEASE} and the {VSDK} v{VSDK_PREVIOUS_RELEASE} in detail.

=== Join a channel

First, create a `Client` object and join a specified channel.

* Use the {VSDK} v{VSDK_PREVIOUS_RELEASE}
+
[source,js]
----
const client = AgoraRTC.createClient({ mode: "live", codec: "vp8" });
client.init("APPID", () => {
  client.join("Token", "Channel", null, (uid) => {
    console.log("join success", uid);
  }, (e) => {
    console.log("join failed", e);
  });
});
----

* Use the {VSDK} v{VSDK_RELEASE}
+
----
const client = AgoraRTC.createClient({ mode: "live", codec: "vp8" });

try {
  const uid = await client.join("APPID", "Channel", "Token", uid);
  console.log("join success");
} catch (e) {
  console.log("join failed", e);
}
----


Here we assume that our code runs within in an `async` function and use `await` in the following code snippets.

Key points:

* In the {VSDK} v{VSDK_RELEASE}, we use the `Promise` object together with `async/await` for the asynchronous operation `join`.
* The {VSDK} v{VSDK_RELEASE} removes the `client.init` method and you pass `APPID` when calling `client.join`. If you want to join another channel that uses a different App ID, you do not need to create another client object.

=== Create an audio track and a video track

Second, create an audio track object from the audio sampled by a microphone and create a local video track from the video captured by a camera. In the sample code, by default, we play the local video track and do not play the local audio track.

* Use the {VSDK} v{VSDK_PREVIOUS_RELEASE}
+
[source,js]
----
const localStream = AgoraRTC.createStream({ audio: true, video: true });
localStream.init(() => {
  console.log("init stream success");
  localStream.play("DOM_ELEMENT_ID", { muted: true });
}, (e) => {
  console.log("init local stream failed", e);
});
----

* Use the {VSDK} v{VSDK_RELEASE}
+
----
const localAudio = await AgoraRTC.createMicrophoneAudioTrack();
const localVideo = await AgoraRTC.createCameraVideoTrack();
console.log("create local audio/video track success");

localVideo.play("DOM_ELEMENT_ID");
----

Key points:

* The {VSDK} v{VSDK_RELEASE} removes the `Stream` object. Call `createMicrophoneAudioTrack` to create an audio track and call `createCameraVideoTrack` to create a video track.
* The audio and video track objects in the {VSDK} v{VSDK_RELEASE} do not provide the `init` method because the SDK initializes the microphone and camera when creating the tracks and notifies you of the result with promises.
* The {VSDK} v{VSDK_RELEASE} can control the playback of local audio and video tracks separately and removes the `muted` parameter in the `play` method. If you do not want to play the local audio track, do not call `play` in the local audio track object.

=== Publish the local audio and video tracks

After creating the local audio and video tracks, publish these tracks to the channel.

* Use the {VSDK} v{VSDK_PREVIOUS_RELEASE}
+
[source,js]
----
client.publish(localStream, err => {
  console.log("publish failed", err);
});
client.on("stream-published",  () => {
  console.log("publish success");
});
----

* Use the {VSDK} v{VSDK_RELEASE}
+
----
try {
  // Remove this line if the channel profile is not live broadcast.
  await client.setClientRole("host");
  await client.publish([localAudio, localVideo]);
  console.log("publish success");
} catch (e) {
  console.log("publish failed", e);
}
----

Key points:

sigh

* If your channel profile is {API-REF-WEB}/interfaces/clientconfig.html#mode[live broadcast], the {VSDK} v{VSDK_PREVIOUS_RELEASE} automatically sets the user role as `host` when publishing the stream. However, in the {VSDK} v{VSDK_RELEASE}, you need to call `setClientRole` to set the user role as `host`.
* In the {VSDK} v{VSDK_RELEASE}, `publish` returns a `Promise` object representing the eventual completion or failure of the asynchronous operation.
* In the {VSDK} v{VSDK_RELEASE}, when calling `publish`, you need to pass one or multiple `LocalTrack` objects instead of the `Stream` object. You can call `publish` repeatedly to publish multiple tracks and call `unpublish` repeatedly to unpublish multiple tracks.

=== Subscribe to remote media tracks and play

When a remote user in the channel publishes media tracks, we need to automatically subscribe to these tracks and play. To do this, you need to listen for the `user-published` event and call `subscribe` when the SDK triggers this event.

* Use the {VSDK} v{VSDK_PREVIOUS_RELEASE}
+
[source,js]
----
client.on("stream-added", e => {
  client.subscribe(e.stream, { audio: true, video: true }, err => {
    console.log("subscribe failed", err);
  });
});

client.on("stream-subscribed", e => {
  console.log("subscribe success");
  e.stream.play("DOM_ELEMENT_ID");
});
----

* Use the {VSDK} v{VSDK_RELEASE}
+
----
client.on("user-published", async (remoteUser, mediaType) => {
  await client.subscribe(remoteUser, mediaType);
  if (mediaType == "video") {
    console.log("subscribe video success");
    remoteUser.videoTrack.play("DOM_ELEMENT_ID");
  }
  if (mediaType == "audio") {
    console.log("subscribe audio success");
    remoteUser.audioTrack.play();
  }
});
----

Key points:

* The {VSDK} v{VSDK_RELEASE} replaces the `stream-added`, `stream-removed`, and `stream-updated` events with the `user-published` and `user-unpublished` events.
+
Pay attention to the `mediaType` parameter of the `user-published` event, which marks the type of the current track the remote user publishes. It can be `&quot;video&quot;` or `&quot;audio&quot;`. If the remote user publishes an audio track and a video track at the same time, the SDK triggers two `user-published` events, in which `mediaType` is `&quot;audio&quot;` and `&quot;video&quot;` respectively.


* In the {VSDK} v{VSDK_RELEASE}, `subscribe` returns a `Promise` object representing the eventual completion or failure of the asynchronous operation. When calling `subscribe`, pass a `remoteUser` object. For details, see {API-REF-WEB}/interfaces/iagorartcremoteuser.html[AgoraRTCRemoteUser].
* When the subscription succeeds, the subscribed tracks are updated to `remoteUser` and you can go on to call `play`.



== What's changed

This section lists the major changes to {VSDK}.

=== Use promises for asynchronous operations

For asynchronous methods such as joining and leaving a channel, publishing and subscribing, enumerating media input devices, and starting and stopping live transcoding, the {VSDK} notifies users of the results of these asynchronous operations with callbacks or events, while the {VSDK} v{VSDK_RELEASE} uses promises.

=== Fine control over audio and video tracks

For the {VSDK} v{VSDK_RELEASE}, we replace the {API-REF-WEB}/interfaces/agorartc.stream.html[Stream] object with `Track` objects. You create, publish, or subscribe to one or multiple audio and video tracks. Tracks make up a stream. The advantage of dividing a stream up into tracks is that audio and video are controllable separately. And these new methods are easier to use than the `Stream.addTrack` and `Stream.removeTrack` methods in the {VSDK}.

For now, the {VSDK} v{VSDK_RELEASE} allows publishing multiple audio tracks but only one video track. The SDK automatically mixes multiple audio tracks into one when publishing.


In addition to replacing the `Stream` object with `Track` objects, the {VSDK} v{VSDK_RELEASE} provides different interfaces for the local and remote tracks. Some methods and objects are only available locally, and some are only available remotely. This change affects multiple API methods, including creating objects, publishing, and subscribing to tracks. For details, see <<user-content-create-an-audio-track-and-a-video-track>> and <<user-content-publish-the-local-audio-and-video-tracks>>.

=== Improved channel events

The improved events are:

* *Rename events*
+
The {VSDK} v{VSDK_RELEASE} ensures the consistency of all event names. For example, Agora renames `onTokenPrivilegeWillExpire` as `token-privilege-will-expire`. Agora also renames several events to ensure developers can better understand how they work. For example, Agora renames `peer-online` and `peer-offline` as `user-joined` and `user-left`, `stream-added` and `stream-removed` as `user-published` and `user-unpublished`.

* *Change the format of parameters in events*
+
In the {VSDK} v{VSDK_PREVIOUS_RELEASE}, the parameters of events must be wrapped in an object, while in the {VSDK} v{VSDK_RELEASE}, you can directly pass multiple parameters in events.
+
Take the `&quot;connection-state-change&quot;` event as an example:

** Use the {VSDK} v{VSDK_PREVIOUS_RELEASE}
+
[source,js]
----
client.on("connection-state-change", e => {
  console.log("current", e.curState, "prev", e.prevState);
});
----

** Use the {VSDK} v{VSDK_RELEASE}
+
----
client.on("connection-state-change", (curState, prevState) => {
  console.log("current", curState, "prev", prevState);
});
----

* *Improve the event notification mechanism*
+
The {VSDK} v{VSDK_RELEASE} improves its event notification mechanism, and does not trigger channel events repeatedly.
+
For example, assume that a local user A and remote users B, C, and D joins a channel at the same time, and B, C, and D all publishes their streams. If A suddenly loses connection with the channel due to bad network conditions, the SDK automatically reconnects A to the channel. While A is reconnecting to the channel, B leaves the channel and C unpublishes the stream. My question is, what events that the SDK triggers during the whole process?
+
When A joins the channel, the {VSDK} v{VSDK_PREVIOUS_RELEASE} and {VSDK} v{VSDK_RELEASE} trigger the same events:

** Events indicating B, C, D joining the channel.
** Events indicating B, C, D publishing their streams.

+
When A loses connection with the channel due to poor network conditions and reconnects to the channel:

** If A uses the {VSDK} v{VSDK_PREVIOUS_RELEASE}: When A loses connection with the channel due to poor network conditions, the SDK assumes A has left the channel and clears all the states in the channel. When A successfully reconnects to the channel, it receives:
** Events indicating C, D joining the channel.
** Events indicating D publishing the stream.

+
In the {VSDK} v{VSDK_PREVIOUS_RELEASE}, you can expect to receive the same events multiple times due to disconnection and reconnection, which may cause unexpected problems for dealing with these events on the app level. You need to listen for connection state changes and reset the states on the app level to avoid unexpected problems when receiving these events for the second time.



** If A uses the {VSDK} v{VSDK_RELEASE}: When A loses connection with the channel due to poor network conditions, the SDK assumes that A is still in the channel and does not clear all the states in the channel. When A successfully reconnects to the channel, the SDK only sends A events that are lost during the reconnection process, including:
** The event indicating B leaving the channel.
** The event indicating C unpublishing the stream.

+
--
The {VSDK} v{VSDK_RELEASE} does not send you the same events repeatedly and ensures your app works properly during reconnection.

As these hypothetical examples demonstrate, the event notification mechanism in the {VSDK} v{VSDK_RELEASE} is more intuitive and does not need extra work.
--

== Core API change list

=== `AgoraRTC`

* Rename `getScreenSources` as {API-REF-WEB}/interfaces/iagorartc.html#getelectronscreensources[getElectronScreenSources], remove callbacks, and return a promise

* {API-REF-WEB}/interfaces/iagorartc.html#getdevices[getDevices] returns a promise. Add {API-REF-WEB}/interfaces/iagorartc.html#getcameras[getCameras] and {API-REF-WEB}/interfaces/iagorartc.html#getmicrophones[getMicrophones]

* Remove the `Logger` object, and add {API-REF-WEB}/interfaces/iagorartc.html#disablelogupload[disableLogUpload], {API-REF-WEB}/interfaces/iagorartc.html#enablelogupload[enableLogUpload], and {API-REF-WEB}/interfaces/iagorartc.html#setloglevel[setLogLevel]

* Replace `createStream` with the following methods:

* {API-REF-WEB}/interfaces/iagorartc.html#createmicrophoneaudiotrack[createMicrophoneAudioTrack]
* {API-REF-WEB}/interfaces/iagorartc.html#createcameravideotrack[createCameraVideoTrack]
* {API-REF-WEB}/interfaces/iagorartc.html#createscreenvideotrack[createScreenVideoTrack]
* {API-REF-WEB}/interfaces/iagorartc.html#createbuffersourceaudiotrack[createBufferSourceAudioTrack]
* {API-REF-WEB}/interfaces/iagorartc.html#createcustomaudiotrack[createCustomAudioTrack]
* {API-REF-WEB}/interfaces/iagorartc.html#createcustomvideotrack[createCustomVideoTrack]

=== `Client`

* Remove `Client.init`

* Remove `Client.getConnectionState` and add {API-REF-WEB}/interfaces/iagorartcclient.html#connectionstate[Client.connectionState]

* Add `RECONNECTING` in {API-REF-WEB}/globals.html#connectionstate[ConnectionState]

* Add {API-REF-WEB}/interfaces/iagorartcclient.html#uid[Client.uid]

* Add {API-REF-WEB}/interfaces/iagorartcclient.html#remoteusers[Client.remoteUsers]

* {API-REF-WEB}/interfaces/iagorartcclient.html#addinjectstreamurl[Client.addInjectStreamUrl] returns a promise representing the success or failure of injecting the stream.Remove `Client.on(&quot;streamInjectStatus&quot;)`.

* Remove the `url` parameter in {API-REF-WEB}/interfaces/iagorartcclient.html#addinjectstreamurl[Client.removeInjectStreamUrl]

* Remove the callbacks of {API-REF-WEB}/interfaces/iagorartcclient.html#enabledualstream[Client.enableDualStream] / {API-REF-WEB}/interfaces/iagorartcclient.html#disabledualstream[Client.disableDualStream] and return a promise

* Move `Client.getCameras`, `Client.getDevices`, `Client.getRecordingDevices` to the `AgoraRTC` interface

* Remove `Client.getPlayoutDevices`

* Replace `Client.getLocalAudioStats`, `Client.getLocalVideoStats`, `Client.getRemoteAudioStats`, and `Client.getRemoteVideoStats` with {API-REF-WEB}/interfaces/ilocalaudiotrack.html#getstats[LocalAudioTrack.getStats], {API-REF-WEB}/interfaces/ilocalvideotrack.html#getstats[LocalVideoTrack.getStats], {API-REF-WEB}/interfaces/iremoteaudiotrack.html#getstats[RemoteAudioTrack.getStats], and {API-REF-WEB}/interfaces/iremotevideotrack.html#getstats[RemoteVideoTrack.getStats].

* Remove `Client.getSystemStats`

* Replace `Client.getSessionStats` and `Client.getTransportStats` with {API-REF-WEB}/interfaces/iagorartcclient.html#getrtcstats[Client.getRTCStats]

* Remove the callback in {API-REF-WEB}/interfaces/iagorartcclient.html#join[Client.join] and return a promise. Also add the `appid` parameter in this method

* Remove the callback in {API-REF-WEB}/interfaces/iagorartcclient.html#leave[Client.leave] and return a promise.

* Add {API-REF-WEB}/interfaces/iagorartcclient.html#once[Client.once]

* Add {API-REF-WEB}/interfaces/iagorartcclient.html#removealllisteners[Client.removeAllListeners]

* Add the `tracks` parameter in {API-REF-WEB}/interfaces/iagorartcclient.html#publish[Client.publish] for passing {API-REF-WEB}/interfaces/ilocaltrack.html[LocalTrack]. This method returns a promise. Remove `Client.on(&quot;stream-published&quot;)`

* {API-REF-WEB}/interfaces/iagorartcclient.html#publish[Client.unpublish] returns a promise.

* Add the `user` parameter in {API-REF-WEB}/interfaces/iagorartcclient.html#subscribe[Client.subscribe] for passing {API-REF-WEB}/interfaces/iagorartcremoteuser.html[AgoraRTCRemoteUser]，This method returns a promise. Remove `Client.on(&quot;stream-subscribed&quot;)`.

* Add the `user` parameter in {API-REF-WEB}/interfaces/iagorartcclient.html#unsubscribe[Client.unsubscribe] for passing {API-REF-WEB}/interfaces/iagorartcremoteuser.html[AgoraRTCRemoteUser]. This method returns a promise.

* {API-REF-WEB}/interfaces/iagorartcclient.html#renewtoken[Client.renewToken] returns a promise.

* Remove the callback in {API-REF-WEB}/interfaces/iagorartcclient.html#setclientrole[Client.setClientRole] and return a promise. In the {VSDK} v{VSDK_RELEASE}, setting the user role as `audience` does not unpublish, and when calling `publish`, the SDK does not automatically setting the user role as `host`

* Replace `Client.setEncryptionMode` and `Client.setEncryptionSecret` with {API-REF-WEB}/interfaces/iagorartcclient.html#setencryptionconfig[Client.setEncryptionConfig]

* {API-REF-WEB}/interfaces/iagorartcclient.html#setlivetranscoding[Client.setLiveTranscoding], {API-REF-WEB}/interfaces/iagorartcclient.html#startlivestreaming[Client.startLiveStreaming], and {API-REF-WEB}/interfaces/iagorartcclient.html#stoplivestreaming[Client.stopLiveStreaming] all return a promise. Remove `Client.on(&quot;liveTranscodingUpdated&quot;)`, `Client.on(&quot;liveStreamingStarted&quot;)`, `Client.on(&quot;liveStreamingFailed&quot;)`, and `Client.on(&quot;liveStreamingStopped&quot;)` events.

* `Client.startChannelMediaRelay` return a promise

* Replace `setDestChannelInfo` in {API-REF-WEB}/interfaces/ichannelmediarelayconfiguration.html[ChannelMediaRelayConfiguration] with {API-REF-WEB}/interfaces/ichannelmediarelayconfiguration.html#adddestchannelinfo[addDestChannelInfo], remove several parameters
* Remove several parameters in {API-REF-WEB}/interfaces/ichannelmediarelayconfiguration.html#setsrcchannelinfo[setSrcChannelInfo] of {API-REF-WEB}/interfaces/ichannelmediarelayconfiguration.html[ChannelMediaRelayConfiguration]
* {API-REF-WEB}/interfaces/iagorartcclient.html#stopchannelmediarelay[Client.stopChannelMediaRelay] return a promise.
* Remove the callback in {API-REF-WEB}/interfaces/iagorartcclient.html#updatechannelmediarelay[Client.updateChannelMediaRelay] and this method returns a promise.
* Replace `Client.on(&quot;first-video-frame-decode&quot;)` and `Client.on(&quot;first-audio-frame-decode&quot;)` with {API-REF-WEB}/interfaces/iremotetrack.html#event_first_frame_decoded[RemoteTrack.on("first-frame-decode")]
* Replace `Client.on(&quot;mute-audio&quot;)`, `Client.on(&quot;mute-video&quot;)`, `Client.on(&quot;unmute-audio&quot;)`, and `Client.on(&quot;unmute-video&quot;)` with {API-REF-WEB}/interfaces/iagorartcclient.html#event_user_mute_updated[Client.on("user-mute-updated")]
* Replace `Client.on(&quot;active-speaker&quot;)` with {API-REF-WEB}/interfaces/iagorartcclient.html#event_volume_indicator[Client.on("volume-indicator")]

* Rename `Client.on(&quot;onTokenPrivilegeWillExpire&quot;)` and `Client.on(&quot;onTokenPrivilegeDidExpire&quot;)` as {API-REF-WEB}/interfaces/iagorartcclient.html#event_token_privilege_will_expire[Client.on("token-privilege-will-expire")] and {API-REF-WEB}/interfaces/iagorartcclient.html#event_token_privilege_did_expire[Client.on("token-privilege-did-expire")]

* Remove `Client.on(&quot;network-type-changed&quot;)`

* Remove `Client.on(&quot;connected&quot;)` and `Client.on(&quot;reconnect&quot;)`. You can get events related to connection states from{API-REF-WEB}/interfaces/iagorartcclient.html#event_connection_state_change[Client.on("connection-state-change")]

* Add the `isFallbackOrRecover` parameter in {API-REF-WEB}/interfaces/iagorartcclient.html#event_stream_fallback[Client.on("stream-fallback")]

____

Remove `Stream.setAudioOutput`. Agora does not recommends setting the audio output device on the web page. Instead, Agora recommends using the default audio output device.

____

=== Stream

In the {VSDK} v{VSDK_RELEASE}, Agora replaces the {API-REF-WEB}/interfaces/agorartc.stream.html[Stream] object with `Track` objects. Tracks make up a stream. You create, publish, or subscribe to one or multiple audio and video tracks to control the media stream. The advantage of dividing a stream up into tracks is that audio and video are controllable separately. Additionally, the {VSDK} v{VSDK_RELEASE} provides different interfaces for the local and remote tracks. Some methods and objects are only available locally, and some are only available remotely.

Listed below are the major differences on media control between the {VSDK} v{VSDK_RELEASE} and {VSDK} v{VSDK_PREVIOUS_RELEASE}:

* Enable/disable a local track: The {VSDK} v{VSDK_RELEASE} replaces the `Stream.muteAudio` and `Stream.muteVideo` methods with {API-REF-WEB}/interfaces/ilocaltrack.html#setenabled[ILocalTrack.setEnabled] for enabling or disabling a local audio or video track. This replacement changes the following SDK behaviors:
* In the {VSDK} v{VSDK_PREVIOUS_RELEASE}, when the mute state changes, the SDK triggers the `Client.on(&quot;mute-audio&quot;)`, `Client.on(&quot;mute-video&quot;)`, `Client.on(&quot;unmute-audio&quot;)`, or `Client.on(&quot;unmute-video&quot;)` events. The {VSDK} v{VSDK_RELEASE} removes these events. When you call `setEnabled` to enable or disable a published local track, the SDK triggers the `Client.on(&quot;user-published&quot;)` or `Client.on(&quot;user-unpublished&quot;)` events on the remote client.
* In the {VSDK} v{VSDK_PREVIOUS_RELEASE}, after you call `Stream.muteVideo(true)` to disable a local video stream, the camera light stays on, which might adversely impact the user experience. In contrast, if you call `setEnabled(false)` in the {VSDK} v{VSDK_RELEASE} to disable a local video track, the SDK immediately turns off the camera.
* Set the media device:
* The {VSDK} v{VSDK_RELEASE} replaces `Stream.switchDevice` with {API-REF-WEB}/interfaces/imicrophoneaudiotrack.html#setdevice[IMicrophoneAudioTrack.setDevice] and {API-REF-WEB}/interfaces/icameravideotrack.html#setdevice[ICameraVideoTrack.setDevice] for setting the media input device.
* The {VSDK} v{VSDK_RELEASE} replaces `Stream.setAudioOutput` with {API-REF-WEB}/interfaces/iremoteaudiotrack.html#setplaybackdevice[IRemoteAudioTrack.setPlaybackDevice] for setting the audio output device.